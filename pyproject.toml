[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "plexctl"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
    "requests>=2.31",
    "tomli>=2.0; python_version < '3.11'",
]

[project.scripts]
plexctl = "plexctl.cli:cli"
